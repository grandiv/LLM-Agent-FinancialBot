# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# Ollama (local LLM) Configuration - Uses native Ollama API
# Make sure Ollama is running: ollama serve
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b
# Note: Ollama native API doesn't require an API key

# Database Configuration
DATABASE_PATH=financial_bot.db

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/bot.log

# MCP Configuration
MCP_EXPORT_DIR=exports
MCP_REMINDERS_FILE=reminders.json

# Web Search MCP (for real-time price lookup)
# Uses Bing → Brave → DuckDuckGo automatic fallback
# No API keys required! Completely free and unlimited!
#
# Setup: Download and install web-search-mcp from https://github.com/mrkrsl/web-search-mcp
# Then set the path to dist/index.js
# Example: WEB_SEARCH_MCP_PATH=C:/mcp-servers/web-search-mcp/dist/index.js
#
WEB_SEARCH_MCP_PATH=

# Optional: Web Search MCP Configuration (defaults work fine)
# WEB_SEARCH_MAX_CONTENT_LENGTH=50000
# WEB_SEARCH_TIMEOUT=6000
# WEB_SEARCH_MAX_BROWSERS=3
# WEB_SEARCH_RELEVANCE_CHECK=true
# WEB_SEARCH_RELEVANCE_THRESHOLD=0.3
